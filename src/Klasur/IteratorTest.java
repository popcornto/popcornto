package Klasur;

import java.util.LinkedList;
import java.util.Iterator;

public class IteratorTest {
   /* public static void main( String[] args )
    {
        //test();
        DoublyLinkedList<Integer> intsmsad = new DoublyLinkedList<>();
        intsmsad.add( 5 );
        intsmsad.add( 6 );
        intsmsad.add( 9 );
        Klasur.Iterator it = intsmsad.iterator();
        while (it.hasNext()){
            System.out.println(it.next());
        }
        System.out.println(hasSmaller(intsmsad, 5));


    }

    private static <F> void printViaIterator( Iterator<F> it )
    {
        while ( it.hasNext() )
        {
            System.out.println( it.next() );
        }
    }
    public static <T extends Comparable<T>> boolean hasSmaller(DoublyLinkedList<T> all, T obj){
        Klasur.Iterator<T> current = all.iterator();
        int count = 0;
        while (current.hasNext()){

            if (obj.compareTo(current.next()) < 0){
                count++;
                break;
            }
        }
        if (count >= 1){
            return true;
        }else {
            return false;
        }
    }

}
*/
}