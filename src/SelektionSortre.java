public class SelektionSortre {
   /* public static int minimum(int[] arr) {
        int min = 0;

        for (int i = 0; i < arr.length; i++) {
            if (min > arr[i]) {
                min = arr[i];
            }
        }
        return min;
    }

    public static int[] SelektionSort(int[] arr) {

        int[] unSortedArray = arr;
        int[] sortedArray = new int[arr.length - 1];
        for (int i = 0; i < arr.length; i++) {

                if (arr[i] == minimum(arr)) {
                    sortedArray[j] = minimum(arr);

                }
            }


        }
*/
}
